part of '../main_page.dart';

class _ListStatusWidget extends StatelessWidget {
  const _ListStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return _ReloadButton(onPressed: () {});
    // return const LoadingWidget();
  }
}
